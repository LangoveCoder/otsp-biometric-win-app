<Page x:Class="BiometricCollegeVerify.Views.ReportsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:BiometricCollegeVerify.Views"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="ReportsView"
      Background="#FAFAFA">

    <Page.Resources>
        <!-- Add converters here in XAML -->
        <local:StatusColorConverter x:Key="StatusColorConverter"/>
        <local:StatusTextConverter x:Key="StatusTextConverter"/>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Verification Reports"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="#2E7D32"
                       VerticalAlignment="Center"/>

            <Button x:Name="RefreshButton"
                    Content="🔄 Refresh"
                    Background="#4CAF50"
                    Foreground="White"
                    Padding="16,10"
                    HorizontalAlignment="Right"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="RefreshButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#43A047"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- Statistics Summary -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="24" Margin="0,0,0,16">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1" Direction="270"/>
            </Border.Effect>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Total Verifications -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Total Verifications" FontSize="12" Foreground="#666" Margin="0,0,0,8"/>
                    <TextBlock x:Name="TotalVerificationsText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#2E7D32"/>
                </StackPanel>

                <!-- Successful -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Successful" FontSize="12" Foreground="#666" Margin="0,0,0,8"/>
                    <TextBlock x:Name="SuccessfulText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#4CAF50"/>
                </StackPanel>

                <!-- Failed -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Failed" FontSize="12" Foreground="#666" Margin="0,0,0,8"/>
                    <TextBlock x:Name="FailedText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#F44336"/>
                </StackPanel>

                <!-- Success Rate -->
                <StackPanel Grid.Column="3">
                    <TextBlock Text="Success Rate" FontSize="12" Foreground="#666" Margin="0,0,0,8"/>
                    <TextBlock x:Name="SuccessRateText" Text="0%" FontSize="24" FontWeight="Bold" Foreground="#2196F3"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Verification Logs -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="16">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1" Direction="270"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <TextBlock Grid.Row="0"
                           Text="Recent Verification Logs"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Margin="8,8,8,16"/>

                <!-- Data Grid -->
                <DataGrid x:Name="LogsDataGrid"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          RowHeight="40"
                          AlternatingRowBackground="#F5F5F5"
                          BorderThickness="0">

                    <DataGrid.Columns>
                        <!-- Date/Time -->
                        <DataGridTextColumn Header="Date/Time" Width="150">
                            <DataGridTextColumn.Binding>
                                <Binding Path="VerificationDateTime" StringFormat="{}{0:MM/dd/yyyy hh:mm tt}"/>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>

                        <!-- Roll Number -->
                        <DataGridTextColumn Header="Roll Number" Width="120">
                            <DataGridTextColumn.Binding>
                                <Binding Path="Student.RollNumber"/>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>

                        <!-- Status -->
                        <DataGridTemplateColumn Header="Status" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding IsSuccessful, Converter={StaticResource StatusColorConverter}}"
                                            CornerRadius="4"
                                            Padding="8,4"
                                            HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding IsSuccessful, Converter={StaticResource StatusTextConverter}}"
                                                   Foreground="White"
                                                   FontWeight="SemiBold"
                                                   FontSize="11"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Type -->
                        <DataGridTextColumn Header="Type" Binding="{Binding VerificationType}" Width="120"/>

                        <!-- Confidence -->
                        <DataGridTextColumn Header="Confidence" Width="100">
                            <DataGridTextColumn.Binding>
                                <Binding Path="MatchConfidence" StringFormat="{}{0}%"/>
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>

                        <!-- Verified By -->
                        <DataGridTextColumn Header="Verified By" Binding="{Binding VerifiedBy}" Width="120"/>

                        <!-- Remarks -->
                        <DataGridTextColumn Header="Remarks" Binding="{Binding Remarks}" Width="*"/>
                    </DataGrid.Columns>

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F5F5F5"/>
                            <Setter Property="Foreground" Value="#333"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            <Setter Property="BorderBrush" Value="#E0E0E0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="12,0"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E8F5E9"/>
                                    <Setter Property="Foreground" Value="#333"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>

                <!-- Empty State -->
                <StackPanel x:Name="EmptyStatePanel"
                            Grid.Row="1"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Visibility="Collapsed">
                    <TextBlock Text="No verification logs yet"
                               FontSize="16"
                               Foreground="#999"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Logs will appear here after verifications"
                               FontSize="12"
                               Foreground="#CCC"
                               HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>