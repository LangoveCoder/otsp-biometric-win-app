<Page x:Class="BiometricCollegeVerify.Views.StudentsListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1200"
      Title="Students List">

    <Page.Resources>
        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Border Grid.Row="0" Background="White" Padding="30,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title -->
                <TextBlock Grid.Row="0" Text="Students List" 
                          FontSize="28" FontWeight="Bold" 
                          Foreground="#212121"/>

                <!-- Search and Filter Bar -->
                <Grid Grid.Row="1" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box -->
                    <Border Grid.Column="0" 
                            Background="#F5F5F5" 
                            CornerRadius="4" 
                            Padding="10"
                            Margin="0,0,15,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="&#x1F50D;" 
                                      FontSize="16" 
                                      VerticalAlignment="Center"
                                      Margin="0,0,10,0"/>
                            <TextBox x:Name="SearchTextBox" 
                                    Grid.Column="1"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    TextChanged="SearchTextBox_TextChanged">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Foreground" Value="#212121"/>
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <VisualBrush Stretch="None" AlignmentX="Left">
                                                            <VisualBrush.Visual>
                                                                <TextBlock Text="Search by roll number, name, or college..." 
                                                                          Foreground="#9E9E9E" 
                                                                          FontSize="14"/>
                                                            </VisualBrush.Visual>
                                                        </VisualBrush>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </Grid>
                    </Border>

                    <!-- Filter Options -->
                    <ComboBox x:Name="FilterComboBox" 
                             Grid.Column="1"
                             Width="150"
                             Height="40"
                             Margin="0,0,15,0"
                             SelectionChanged="FilterComboBox_SelectionChanged">
                        <ComboBoxItem Content="All Students" IsSelected="True"/>
                        <ComboBoxItem Content="With Fingerprint"/>
                        <ComboBoxItem Content="Without Fingerprint"/>
                        <ComboBoxItem Content="Verified"/>
                        <ComboBoxItem Content="Not Verified"/>
                    </ComboBox>

                    <!-- Refresh Button -->
                    <Button x:Name="RefreshButton" 
                           Grid.Column="2"
                           Content="🔄 Refresh" 
                           Style="{StaticResource PrimaryButton}"
                           Click="RefreshButton_Click"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="30,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Statistics Cards -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Total Students Card -->
                    <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,15,0">
                        <StackPanel>
                            <TextBlock Text="Total Students" 
                                      FontSize="14" 
                                      Foreground="#757575" 
                                      Margin="0,0,0,5"/>
                            <TextBlock x:Name="TotalStudentsText" 
                                      Text="0" 
                                      FontSize="32" 
                                      FontWeight="Bold" 
                                      Foreground="#2196F3"/>
                        </StackPanel>
                    </Border>

                    <!-- With Fingerprint Card -->
                    <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="0,0,15,0">
                        <StackPanel>
                            <TextBlock Text="With Fingerprint" 
                                      FontSize="14" 
                                      Foreground="#757575" 
                                      Margin="0,0,0,5"/>
                            <TextBlock x:Name="WithFingerprintText" 
                                      Text="0" 
                                      FontSize="32" 
                                      FontWeight="Bold" 
                                      Foreground="#4CAF50"/>
                        </StackPanel>
                    </Border>

                    <!-- Verified Card -->
                    <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="0,0,15,0">
                        <StackPanel>
                            <TextBlock Text="Verified" 
                                      FontSize="14" 
                                      Foreground="#757575" 
                                      Margin="0,0,0,5"/>
                            <TextBlock x:Name="VerifiedStudentsText" 
                                      Text="0" 
                                      FontSize="32" 
                                      FontWeight="Bold" 
                                      Foreground="#FF9800"/>
                        </StackPanel>
                    </Border>

                    <!-- Pending Card -->
                    <Border Grid.Column="3" Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="Pending" 
                                      FontSize="14" 
                                      Foreground="#757575" 
                                      Margin="0,0,0,5"/>
                            <TextBlock x:Name="PendingStudentsText" 
                                      Text="0" 
                                      FontSize="32" 
                                      FontWeight="Bold" 
                                      Foreground="#F44336"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Students DataGrid -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                    <DataGrid x:Name="StudentsDataGrid" 
                             AutoGenerateColumns="False"
                             IsReadOnly="True"
                             GridLinesVisibility="Horizontal"
                             HeadersVisibility="Column"
                             RowHeight="120"
                             SelectionMode="Single"
                             SelectionUnit="FullRow"
                             CanUserResizeRows="False"
                             CanUserSortColumns="True"
                             AlternatingRowBackground="#FAFAFA"
                             Background="White"
                             BorderThickness="0">

                        <DataGrid.Resources>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Foreground" Value="#212121"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="Padding" Value="15,10"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                            </Style>

                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F5F5F5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>

                            <Style TargetType="DataGridCell">
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="15,10"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGrid.Resources>

                        <DataGrid.Columns>
                            <!-- Student Photo Column -->
                            <DataGridTemplateColumn Header="Photo" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Width="80" Height="80" 
                                               Background="#F5F5F5" 
                                               CornerRadius="4"
                                               BorderBrush="#E0E0E0"
                                               BorderThickness="1">
                                            <Grid>
                                                <Image Stretch="UniformToFill"
                                                      Visibility="{Binding StudentPhotoVisibility}">
                                                    <Image.Source>
                                                        <Binding Path="StudentPhotoImage"/>
                                                    </Image.Source>
                                                </Image>
                                                <TextBlock Text="No Photo" 
                                                          FontSize="10"
                                                          Foreground="#9E9E9E"
                                                          TextAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Visibility="{Binding PhotoPlaceholderVisibility}"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Fingerprint Image Column -->
                            <DataGridTemplateColumn Header="Fingerprint" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Width="100" Height="100" 
                                               Background="#F5F5F5" 
                                               CornerRadius="4"
                                               BorderBrush="#E0E0E0"
                                               BorderThickness="1">
                                            <Grid>
                                                <!-- Fingerprint Image -->
                                                <Image x:Name="FingerprintImg" 
                                                      Stretch="Uniform"
                                                      Visibility="{Binding FingerprintImageVisibility}">
                                                    <Image.Source>
                                                        <Binding Path="FingerprintImage"/>
                                                    </Image.Source>
                                                </Image>

                                                <!-- Placeholder when no fingerprint -->
                                                <TextBlock Text="No Fingerprint" 
                                                          FontSize="11"
                                                          Foreground="#9E9E9E"
                                                          TextAlignment="Center"
                                                          TextWrapping="Wrap"
                                                          VerticalAlignment="Center"
                                                          Visibility="{Binding PlaceholderVisibility}"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Roll Number Column -->
                            <DataGridTextColumn Header="Roll Number" 
                                              Binding="{Binding RollNumber}" 
                                              Width="150">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontSize" Value="16"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="#2196F3"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Name Column -->
                            <DataGridTextColumn Header="Name" 
                                              Binding="{Binding Name}" 
                                              Width="200">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontSize" Value="14"/>
                                        <Setter Property="Foreground" Value="#212121"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- College Column -->
                            <DataGridTextColumn Header="College" 
                                              Binding="{Binding CollegeName}" 
                                              Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontSize" Value="13"/>
                                        <Setter Property="Foreground" Value="#616161"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Test Column -->
                            <DataGridTextColumn Header="Test" 
                                              Binding="{Binding TestName}" 
                                              Width="150">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontSize" Value="13"/>
                                        <Setter Property="Foreground" Value="#616161"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Verification Status Column -->
                            <DataGridTemplateColumn Header="Status" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding StatusBackground}" 
                                               CornerRadius="12" 
                                               Padding="10,5">
                                            <TextBlock Text="{Binding StatusText}" 
                                                      FontSize="12"
                                                      FontWeight="Bold"
                                                      Foreground="{Binding StatusForeground}"
                                                      TextAlignment="Center"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Template Size Column -->
                            <DataGridTextColumn Header="Template Size" 
                                              Binding="{Binding TemplateSizeText}" 
                                              Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontSize" Value="12"/>
                                        <Setter Property="Foreground" Value="#757575"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- Loading Overlay -->
        <Grid x:Name="LoadingOverlay" 
              Grid.Row="0" 
              Grid.RowSpan="2"
              Background="#80000000" 
              Visibility="Collapsed">
            <Border Background="White" 
                   CornerRadius="8" 
                   Padding="30"
                   Width="300"
                   Height="150"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="&#x23F3;" 
                              FontSize="48" 
                              HorizontalAlignment="Center"
                              Margin="0,0,0,15"/>
                    <TextBlock x:Name="LoadingText" 
                              Text="Loading students..." 
                              FontSize="16" 
                              Foreground="#212121"
                              TextAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
