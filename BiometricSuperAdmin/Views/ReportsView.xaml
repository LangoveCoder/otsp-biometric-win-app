<Page x:Class="BiometricSuperAdmin.Views.ReportsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Reports"
      Background="#FAFAFA">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Reports"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Foreground="#2E7D32"/>
            <Button x:Name="RefreshButton"
                    Content="🔄 Refresh"
                    Background="#4CAF50"
                    Foreground="White"
                    Padding="16,10"
                    HorizontalAlignment="Right"
                    Click="RefreshButton_Click"/>
        </Grid>

        <!-- Statistics -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="24" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Total Students" FontSize="12" Foreground="#666" Margin="0,0,0,8"/>
                    <TextBlock x:Name="TotalVerificationsText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#2E7D32"/>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Verified" FontSize="12" Foreground="#666" Margin="0,0,0,8"/>
                    <TextBlock x:Name="SuccessfulText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#4CAF50"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Pending" FontSize="12" Foreground="#666" Margin="0,0,0,8"/>
                    <TextBlock x:Name="FailedText" Text="0" FontSize="24" FontWeight="Bold" Foreground="#F44336"/>
                </StackPanel>

                <StackPanel Grid.Column="3">
                    <TextBlock Text="Success Rate" FontSize="12" Foreground="#666" Margin="0,0,0,8"/>
                    <TextBlock x:Name="SuccessRateText" Text="0%" FontSize="24" FontWeight="Bold" Foreground="#2196F3"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Logs -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="16">
            <Grid>
                <DataGrid x:Name="LogsDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date/Time" Binding="{Binding VerificationDateTime}" Width="150"/>
                        <DataGridTextColumn Header="Roll Number" Binding="{Binding Student.RollNumber}" Width="120"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding VerificationType}" Width="100"/>
                        <DataGridTextColumn Header="Result" Binding="{Binding IsSuccessful}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel x:Name="EmptyStatePanel"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Visibility="Collapsed">
                    <TextBlock Text="No logs yet" FontSize="16" Foreground="#999"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Page>