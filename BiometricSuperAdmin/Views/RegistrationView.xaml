<Page x:Class="BiometricSuperAdmin.Views.RegistrationView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="768" d:DesignWidth="1024"
      Background="#FAFAFA">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Title -->
        <TextBlock Grid.Row="0" 
                   Text="Student Registration"
                   FontSize="28"
                   FontWeight="SemiBold"
                   Foreground="#1976D2"
                   Margin="0,0,0,24"/>

        <!-- Context Information Card -->
        <Border Grid.Row="1" 
                Background="White" 
                CornerRadius="8" 
                Padding="24"
                Margin="0,0,0,24">
            <Border.Effect>
                <DropShadowEffect Color="#000000" 
                                  Opacity="0.1" 
                                  BlurRadius="16" 
                                  ShadowDepth="2" 
                                  Direction="270"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Current Registration Context" 
                           FontSize="16" 
                           FontWeight="SemiBold"
                           Foreground="#333"
                           Margin="0,0,0,16"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="College:" FontWeight="SemiBold" Foreground="#666" Margin="0,0,8,0"/>
                    <TextBlock Grid.Column="1" x:Name="CollegeTextBox" Text="-" Foreground="#333" Margin="0,0,24,0"/>

                    <TextBlock Grid.Column="2" Text="Test:" FontWeight="SemiBold" Foreground="#666" Margin="0,0,8,0"/>
                    <TextBlock Grid.Column="3" x:Name="TestTextBox" Text="-" Foreground="#333" Margin="0,0,24,0"/>

                    <TextBlock Grid.Column="4" Text="Device:" FontWeight="SemiBold" Foreground="#666" Margin="0,0,8,0"/>
                    <TextBlock Grid.Column="5" x:Name="DeviceTextBox" Text="-" Foreground="#333"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Registration Form -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="8" 
                Padding="32"
                VerticalAlignment="Top">
            <Border.Effect>
                <DropShadowEffect Color="#000000" 
                                  Opacity="0.1" 
                                  BlurRadius="16" 
                                  ShadowDepth="2" 
                                  Direction="270"/>
            </Border.Effect>

            <StackPanel MaxWidth="600">
                <!-- Instructions -->
                <TextBlock Text="Enter student roll number and capture fingerprint" 
                           FontSize="14"
                           Foreground="#666"
                           Margin="0,0,0,24"/>

                <!-- Roll Number Input -->
                <TextBlock Text="Roll Number *" 
                           FontWeight="SemiBold"
                           Foreground="#333"
                           Margin="0,0,0,8"/>
                <TextBox x:Name="RollNumberTextBox"
                         FontSize="16"
                         Padding="12"
                         Background="#F5F5F5"
                         BorderBrush="#E0E0E0"
                         BorderThickness="1"
                         Margin="0,0,0,24"/>

                <!-- Fingerprint Section -->
                <TextBlock Text="Fingerprint Capture" 
                           FontWeight="SemiBold"
                           Foreground="#333"
                           Margin="0,0,0,8"/>

                <Border Background="#E3F2FD" 
                        CornerRadius="8" 
                        Padding="24"
                        Margin="0,0,0,24">
                    <StackPanel>
                        <TextBlock Text="📌 Fingerprint will be captured automatically when you click Register" 
                                   FontSize="14"
                                   Foreground="#1976D2"
                                   TextWrapping="Wrap"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="(Using simulated fingerprint for testing)" 
                                   FontSize="12"
                                   Foreground="#666"
                                   TextWrapping="Wrap"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Buttons -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            x:Name="RegisterButton"
                            Content="Register Student"
                            Click="RegisterButton_Click"
                            Background="#4CAF50"
                            Foreground="White"
                            FontSize="16"
                            FontWeight="SemiBold"
                            Padding="24,12"
                            BorderThickness="0"
                            Cursor="Hand"
                            Margin="0,0,12,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#4CAF50"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="6"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#45A049"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#BDBDBD"/>
                                        <Setter Property="Cursor" Value="No"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Grid.Column="1"
                            x:Name="ClearButton"
                            Content="Clear"
                            Click="ClearButton_Click"
                            Background="#757575"
                            Foreground="White"
                            FontSize="16"
                            FontWeight="SemiBold"
                            Padding="24,12"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#757575"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="6"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#616161"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="3"
                x:Name="LoadingOverlay"
                Background="#99000000"
                Visibility="Collapsed">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="⏳" 
                           FontSize="48" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,16"/>
                <TextBlock Text="Registering student..." 
                           FontSize="18" 
                           Foreground="White"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>